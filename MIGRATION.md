# 从旧选股系统迁移到 v2

## 保持不变的策略意图

- 月线趋势过滤；
- 周线MA多头、MACD金叉或企稳；
- 周线阴转阳反转与双阳效率提升；
- 周线放量阴线否决；
- 日线回踩MA10/MA20；
- 缩量涨幅放大；
- 两日上涨加速；
- 板块模式可跳过weeksurge直接筛日线买点。

## 有意改变的行为

| 旧行为 | v2行为 |
|---|---|
| `weekly.iloc[-1]`含义随数据更新状态变化 | 当前周和完成周按日期显式切分 |
| 周一用上周周线开盘配实时价 | 周一用腾讯今开作为本周开盘 |
| 腾讯“手”与TDX“股”直接比较 | 实时入口统一乘100为“股” |
| 周三/周四把价格涨幅线性放大到五天 | 价格不外推，仅成交量按进度估算 |
| 上午累计量与昨日全天量比较 | 同刻同比优先；缺失时使用并标记全天投影 |
| 未过weeksurge股票均写入“阴线” | `surge_green`只保留实时价低于昨收 |
| 多个文件复制三套surge逻辑 | 唯一`strategies/surge.py` |
| 异常静默跳过 | 逐票拒绝原因+汇总诊断JSON |
| CSV直接覆盖 | 临时文件完成后原子替换 |
| B股/ST默认混入 | 默认排除，可在YAML显式放开 |

## 尚未自动切换的部分

- crontab仍指向旧交付版的板块分析；
- nginx简报仍由旧系统生成；
- 旧CSV文件名未建立兼容软链接；
- v2未自动下单，也不应在完成策略验证前接入自动交易。

## 第二轮加固（对照外部审阅补齐）

- 腾讯报价时间戳（f[30]）已解析并用于时效检查；集合竞价前/停牌（今开或量为0）不产生盘中信号。
- 北交所920xxx已纳入代码范围与行情前缀映射；B股仍默认排除。
- `state/intraday_volume_snapshots.csv` 持久化同刻累计量，下一交易日自动匹配±5分钟内最近快照；匹配不到才走带标记的全天投影。
- 每次运行输出不可覆盖归档到 `output/runs/<时间戳>/<模式>/`。
- 单次运行内日线读取改为进程内缓存（每股一次，不再逐阶段重读全量历史）。
- `SPEC.md` 固化规则文字规范（版本2.0.0-draft1）。

## 已知后续事项（有意不做在本次）

- 交易日历节假日表需人工按年维护（配置项 `calendar.holidays` 预留，当前为空，靠新鲜度闸门兜底）。
- 与冻结的 2026-08-07 旧版输出做 golden-file 对比：规则已按修复项改变，逐差异需溯源到具体缺陷，建议在影子运行期执行。
- 跨进程持久日线缓存（parquet）未做：当前为单次运行内缓存，满足现有运行节奏。
- 旧文件名兼容层（`--legacy-names`）未做：并行期直接读 v2 原生文件名。
