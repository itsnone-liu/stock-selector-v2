# MULTIPERIOD_CONDITION_RISKSET_V2 — 独立映射+共同风险集合+S3 配对覆盖预检(2026-09-22)

授权(九轮复审): 修正映射+重建共同风险集合+无结果变量配对覆盖预检;
未运行收益比较/预测模型/回测; Y40 数值未参与任何判定。

## 一、P0 修复: 独立代码映射

| 项 | 旧(阶段0) | 新(v2) |
|---|---|---|
| 映射源 | identify 事件名单(结果相关) | **冻结库 per_stock 文件名主键 5,240(独立)** |
| l1 流失 | 401(全为无事件股) | **0** |
| 台账 | l1-l6 闭合 | **{}(零流失)** |
| 风险股票-日 | 294,722(旧保留) | **323,031**(含首突日资格窗) |
| 首突 | 27,310 | **27,310, 全部在共同风险集(资格一致)** |

窗口定义: 有 bo 段 [anchor,bo] 含首突日(当日收盘确认突破仍属风险集合);
无 bo 段 [anchor, min(end_day, anchor+120)) 不含 end 日; 冲突断言=0。

## 二、披露: 复权因子缺失

冻结库全量暴露缺失 28,944 行=全库 6,696,221 日线行 **0.432%**
(identify 4,760 子集时为 0); 相关日已跳过不生成价格(不默认 F=1); 入 JSON 披露字段。

## 三、S3 配对覆盖预检(无结果变量; 对照含此后突破者不剔除)

| 项 | 值 |
|---|---|
| 配对日(事件≥1 且对照≥10 不同股票) | **643** |
| 事件组规模 | p50=24 股/日, max=489 |
| 对照组规模 | p50=311 股/日 |
| 每股贡献风险日 | p50=65, p90=107, max=229 |
| 事件日折(月桶)分布 | 18-23 桶各 44-126 日 |

配对可行性充足; 每股贡献日分布确认重复截面结构(同股中位 65 日)——
S2 股票块聚集校正是必需的推断机制(九轮复审定位)。

## 四、边界

开发性研究; 无结果变量参与本预检; 非因果; 对照事后突破不剔除不等于两组可比;
平衡表(背景变量分布差异, 无结果变量)在 v0.2 冻结后先行; 已封存实验未动。

## 五、十轮复审对账与审计补章(2026-09-22)

### 112 条首突流失对账(已闭合)

首突流失台账 v2: **l6a_bo_day_factor_missing=112**(bo 日有行情行但复权因子
缺失→价格跳过); l6b(行情无 bo 日行)=0。即 112 全部为因子缺失所致, 非结果相关
筛选(缺失源于因子表覆盖, 与个股后续表现无关); 旧阶段0 同口径 112 一致。

### 复权因子元数据修正

JSON 旧字段 factor_missing_assert="executed(==0)" 为口径残留, 已删除; 替换
factor_missing_handling="skipped_not_silent(不默认F=1); 共 28,944 行已披露"。

### 缺失分布审计(不止总体 0.432%)

| 维度 | 结果 |
|---|---|
| 有缺失的股票 | **24 只**(占 5,240 的 0.46%) |
| top3 | sz.000970/sh.600313/sz.000981 各 1,386 行(近全历史缺失) |
| 风险集内被跳过的股票-日 | **0**(缺失日不落入共同风险集) |
| 首突日缺失(事件组排除) | **112**(=l6a, 见上) |

结论: 缺失集中于 24 只长缺失股; 对照暴露零污染; 事件组仅首突日完整性受影响,
112 条排除为数据完整性口径, 须在主检验样本定义中披露。

### 配对覆盖 vs Y40 窗口资格(区分表述, 十轮复审)

| 口径 | 数 |
|---|---|
| 配对资格日(事件>=1 且对照>=10, 全区间 b18-b23) | **643** |
| 其中 b19-b22(原四测试折) | **483** |
| Y40 窗口资格日(d+40 在指数日历内) | **618** |
| 其中 b19-b22 | **483** |

即: 643=配对资格通过日数(非最终 Y40 可检验数); b19-b22 内 483 日配对且窗口
完整; 价格级缺失处理(停牌/缺价)沿用 Y40 冻结口径在主检验执行——最终可检验
样本量届时确定。
