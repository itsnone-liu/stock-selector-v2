# 选股规则规范

版本：`2.0.0-draft1`

本文件是规则含义的唯一文字来源；阈值的唯一机器来源是 `config/default.yaml`。

## 1. 时间和数据定义

- `asof`：本次运行的显式判断时点；未传入时为当前时间。
- 已完成周：`asof`所在自然周周一之前的日线，按`W-FRI`聚合。
- 当前周：本周周一至`asof`的本地日线，加可用的实时价、今开和当日累计量。
- 涨幅：价格真实已实现涨幅，不向未来交易日外推。
- 周量进度：已完成交易日加当日已交易时段占比，再除以标准五日；仅用于成交量估算。
- 标准成交量单位：股；腾讯“手”在数据适配边界乘100。

## 2. 数据闸门

- 日线缺失、过期或出现未来数据时跳过并记录原因。
- 实时报价必须有合法最新价、昨收、今开、成交量和可解析时间戳。
- 报价超过配置的最大时效时跳过。
- 实时价为0、今开为0或成交量为0视为停牌/集合竞价前无有效成交，不产生盘中信号。

## 3. 风险范围

默认排除B股、ST/*ST/退市标记、上市时间过短和低流动性股票。北交所支持4/8/920前缀。过滤结果必须进入诊断统计。

## 4. 月线趋势

硬条件：MA5 > MA10 > MA20，且最近月跌幅不低于配置下限。至少需要20根月线，避免MA20为NaN时静默淘汰。

## 5. 周线趋势

允许三类信号：

1. MA5 > MA10 > MA20；
2. DIF由不高于DEA变为高于DEA；
3. 零轴下MACD柱由恶化转改善，DIF/DEA差距按股价归一后低于阈值。

## 6. 周线短期形态

先对最后一个已完成周执行放量阴线否决：收阴且成交额（无成交额时用成交量）达到此前4周均值的1.5倍。

随后当前周必须收阳，并默认要求当前价高于上一完成周收盘。允许：

- 阴转阳反转：上一完成周阴，当前周阳；
- 双阳效率提升：上一完成周阳，当前周阳，且当前周`|涨幅|/预计量比`高于上一周对应值。

“预计量比”只对成交量使用周内进度估算，不对价格涨幅做五日外推。

## 7. 日线买点

优先级固定：

1. 回踩不破：当日涨跌位于配置区间，价格接近MA10或MA20；盘后可进一步用最低价确认，盘中只属于“均线附近”近似信号。
2. 缩量涨幅放大：今日上涨、涨幅大于昨日、低于追涨上限，且同刻量比小于1；无昨日同刻快照时仅允许使用带`projected_full_day`标记的投影回退。
3. 两日上涨加速：昨日阳线、今日上涨、今日涨幅不小于昨日且低于追涨上限。

综合分为上游周线形态分、买点基础分和技术加分之和；这是排序分，不是概率。

## 8. 板块模式

板块模式按既定业务要求跳过weeksurge，但仍执行数据新鲜度、风险过滤和统一日线买点。输出会记录`board_mode_skips_surge`，避免与全市场三层筛选混淆。

## 9. 输出语义

- `surge_*`：通过周线短期形态；
- `buy_*`：通过最终买点；
- `surge_green_realtime`：仅指通过surge且实时价低于昨收；
- `rejections_*`：逐票、逐阶段的最终淘汰原因；
- `diagnostics_*`：阶段计数、报价错误、同刻量参考覆盖数；
- `output/runs/<timestamp>/`：每次运行不可覆盖的归档。
